# Session Changelog — 2026-09-04

**Scope:** `services/backend-py`, `apps/web`, root config, docs.
**Method:** every change was verified live against the running stack (backend on `:4000`, admin app on `:5002`, PostgreSQL `camtechStore`), not just by inspection.
**Status:** ✅ all items complete and verified.

---

## 1. Backend — write paths (all live-verified)

| Change | Files | Result |
|---|---|---|
| Verified the **enum-binding sweep** is complete — all 33 native Postgres enum types bound via `pg_enum()` | [`app/core/db_enums.py`](../../services/backend-py/app/core/db_enums.py) + module models | 62 tables + 108 routes import clean; `POST /products`, `/locations`, `/sales` all `200` |
| Added **`POST /customers`** (was `405`) plus `PATCH`/`PUT`, with enum validation (bad `type` → clean `400`) and per-tenant code uniqueness (→ `409`) | [`customers/api.py`](../../services/backend-py/app/modules/customers/api.py), [`customers/schemas.py`](../../services/backend-py/app/modules/customers/schemas.py) | create `201`, update `200`, verified live |
| Implemented real **`GET /reports/summary`** + **`/reports/export`** computed from live sales/inventory/locations; removed the hardcoded mock `/reports/dashboard` | [`reporting/api.py`](../../services/backend-py/app/modules/reporting/api.py) | Reports page shows real figures (matches dashboard: 29 orders, AOV $147.98) instead of `$0.00`/`404` |

## 2. Frontend

| Change | Files | Result |
|---|---|---|
| Fixed **`/customers` routing bug** — `startsWith('/customer')` sent the admin CRM to the customer-portal shell | [`apps/web/src/App.tsx`](../../apps/web/src/App.tsx), [`components/domain-bar.tsx`](../../apps/web/components/domain-bar.tsx) | `/customers` now loads the CRM; domain bar shows `admin.camtech.cam` |
| Added a **React error boundary** around the routed experience (nav stays outside it) | [`components/error-boundary.tsx`](../../apps/web/components/error-boundary.tsx) (new), `App.tsx` | a crash in one screen is contained; other routes keep working |

## 3. Full microservices split ("all is microservice")

| Change | Files | Result |
|---|---|---|
| Added a 7th **Platform & Experience service** (`:4007`) owning every domain without a core service (reporting, workflows, tickets, notifications, projects, documents, automations, industry, AI, events, app registry, outbox) | [`microservices/platform_service.py`](../../services/backend-py/app/microservices/platform_service.py) (new) | every `/api/v1` route is now owned by a service |
| Rewired the gateway to route **every** prefix to a service (platform = catch-all) | [`microservices/gateway.py`](../../services/backend-py/app/microservices/gateway.py) | no route depends on the monolith in normal operation |
| Made the in-process fallback **lazy + guarded** (`get_fallback_app()`) | `gateway.py` | a syntax/import error in any one module can no longer stop the gateway from booting |
| Gave **every service** the same response envelope, error handlers, and full SQLAlchemy registry via the shared factory | [`microservices/common.py`](../../services/backend-py/app/microservices/common.py) | fixes raw (un-enveloped) responses and `KeyError: 'Organization'` 500s when a service mounts only some models |
| Added `ms:platform`, included it in `ms:all`; added `platform-service` to Docker Compose | [`package.json`](../../package.json), [`docker-compose.yml`](../../docker-compose.yml) | `pnpm ms:all` starts gateway + 7 services |

## 4. Verification performed

- **Reads:** 44/47 parameter-free GETs `200` (the 3 others are param-required endpoints + the SSE stream).
- **Writes:** every create endpoint `200/201` against the live DB.
- **Microservices end-to-end:** all 8 processes healthy; each domain routed to its own service; responses enveloped; the admin frontend loads real data through the gateway.
- **Fault isolation (live):** a syntax error injected into the delivery module took down **only** `:4004` — the gateway and the other six services stayed up, admin/POS flows all returned `200`, and the broken route returned a clean `503`. The error was then reverted and all 8 services confirmed healthy.

## 5. Notes / follow-ups

- `services/backend-py/app/routers/enterprise_routes.py` is **dead code** (not imported/mounted anywhere) and still contains a hardcoded mock `/reports/dashboard`; recommend deleting it.
- Add a CI **drift-guard** step (`scripts/schema_audit.py`) and a CI check that every module imports, so a broken module is caught before deploy.
- The stack was run without `--reload` for stability during verification; use `pnpm ms:all` for hot-reload development.
